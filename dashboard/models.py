# Dashboard uses models from other apps; no custom models needed.
# This file can remain empty or have a placeholder.
from django.db import models
# No custom models for dashboard - it aggregates data from other apps
